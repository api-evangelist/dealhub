generated: '2026-08-12'
method: generated
source: openapi/ (all 11 DealHub OpenAPI documents) + https://developers.dealhub.io/docs/
note: >-
  DealHub publishes no AGENTS.md and no provider-authored agent skills; the developer
  portal index contains no skills entry. These three are generated by API Evangelist
  from the eleven harvested OpenAPI documents and the published guides. Every
  operationId referenced was verified to exist verbatim in the spec named on the skill.
skills:
- file: dealhub-headless-quote-generation.md
  name: Generate and publish a DealHub quote headlessly
  api: openapi/dealhub-headless-api-openapi.yml
  operations: [getVersions, getGenerateQuoteTemplate, simulateQuote, generateQuote,
    submitQuote, publishQuote, signQuoteExternally, getQuoteById, getQuoteDocument]
- file: dealhub-version-and-catalog-management.md
  name: Manage DealHub Versions and the product catalog
  api: openapi/dealhub-version-api-openapi.yml
  operations: [getVersions, getVersionById, duplicateVersion, activateVersion,
    getProductCatalog, getVersionProducts, getProductsBySku, uploadProductCatalog,
    updateProductCatalogItems, getAsyncRequestStatus, getAsyncRequestSummary,
    exportPlaybookData]
- file: dealhub-crm-handoff-and-import.md
  name: Hand a CRM user into DealHub and import CRM accounts
  api: openapi/dealhub-crm-api-openapi.yml
  operations: [authenticateUser, createQuote, openQuote, viewQuotesForOpportunity,
    viewMyOpportunities, authenticatePartnerUser, createPartnerQuote, openPartnerQuote,
    openPartnerOpportunity, startCrmImport, getCrmImportStatus, getCrmImportDetail,
    lookupCrmImportId, retryCrmImport]
